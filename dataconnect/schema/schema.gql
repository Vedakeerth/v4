type User @table {
  displayName: String!
  createdAt: Timestamp!
  email: String
  photoUrl: String
}

type Movie @table {
  title: String!
  year: Int!
  createdAt: Timestamp!
  summary: String
  genres: [String]
  runtime: Int
}

type Watch @table {
  createdAt: Timestamp!
  watchDate: Date!
  location: String
  user: User
  movie: Movie
}

type Review @table {
  rating: Int!
  createdAt: Timestamp!
  updatedAt: Timestamp!
  reviewText: String
  movie: Movie
  user: User
  watch: Watch
}

type List @table {
  name: String!
  isPublic: Boolean!
  createdAt: Timestamp!
  updatedAt: Timestamp!
  description: String
  user: User
}

type ListItem @table(key: ["list", "movie"]) {
  position: Int!
  note: String
  list: List!
  movie: Movie!
}